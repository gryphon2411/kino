apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "derkino-services.fullname" . }}-test-connection"
  labels:
    {{- include "derkino-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "derkino-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
