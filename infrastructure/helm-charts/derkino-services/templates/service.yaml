apiVersion: v1
kind: Service
metadata:
  name: {{ include "derkino-services.fullname" . }}
  labels:
    {{- include "derkino-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "derkino-services.selectorLabels" . | nindent 4 }}
