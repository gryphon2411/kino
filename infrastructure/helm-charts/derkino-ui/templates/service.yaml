apiVersion: v1
kind: Service
metadata:
  name: {{ include "derkino-ui.fullname" . }}
  labels:
    {{- include "derkino-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "derkino-ui.selectorLabels" . | nindent 4 }}
