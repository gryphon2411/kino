apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Chart.Name }}-network-policy
  namespace: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Chart.Name }}
  policyTypes:
  - Ingress
  ingress:
  # Allow traffic from derkino application namespace
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: derkino
    ports:
    - port: 9092
      protocol: TCP
    - port: 2181
      protocol: TCP
  # Allow traffic from migration namespace during migration
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: infrastructure-parallel
    ports:
    - port: 9092
      protocol: TCP
    - port: 2181
      protocol: TCP
  # Deny all other traffic
  - from: []