# Default variables for Helm role

# Namespace for infrastructure components
helm_infrastructure_namespace: "infrastructure"

# Path to Helm charts directory
helm_charts_path: "{{ lookup('env', 'PWD') }}/infrastructure/helm-charts/derkino-infrastructure"

# Helm cache directory
helm_cache_dir: "{{ lookup('env', 'HOME') }}/.cache/helm"
